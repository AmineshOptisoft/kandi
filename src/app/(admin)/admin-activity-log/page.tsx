import type { Metadata } from "next";
// import AdminActivityLog from "@/components/admins/AdminActivityLog";

export const metadata: Metadata = {
  title: "Admin Activity Log | TePay Admin",
  description: "Monitor and audit actions performed by system administrators",
};

export default function AdminActivityLogPage() {
  // return <AdminActivityLog />;
}
